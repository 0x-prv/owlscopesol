import TokenList from "@/components/TokenList";
import { supabaseAdmin } from "@/lib/server/supabase-admin";
export const dynamic = "force-dynamic";
export default async function TrendingPage() { const { data } = await supabaseAdmin.from("tokens").select("mint_address,name,symbol,last_updated_at,token_snapshots(price_usd,market_cap_usd,liquidity_usd,volume_24h_usd,snapshot_at),risk_reports(overall_risk_score,overall_risk_label,confidence)").order("last_updated_at", { ascending: false }).limit(50); return <main className="mx-auto max-w-5xl px-4 py-10"><h1 className="font-display text-3xl font-semibold">Trending signals</h1><p className="mt-2 text-sm text-muted">Ranked by available real internal updates and market signals. Missing metrics remain unavailable.</p><div className="mt-6"><TokenList rows={(data ?? []) as []} /></div></main>; }
